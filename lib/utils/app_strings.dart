/// Non-translatable, static string identifiers for the app.
/// All UI-facing strings belong in AppLocalizations (context.l10n).
class AppStrings {
  AppStrings._();

  // Brand identifiers
  static const String appName = 'ZenPulse';
  static const String contactEmail = 'support@zenpulse.app';
  static const String appDomain = 'zenpulse.app';
}
